# .env
# Configuration for Claude on Vertex AI

# --- Required Settings ---
# Replace with your actual Google Cloud Project ID.
export ANTHROPIC_VERTEX_PROJECT_ID="your-gcp-project-id"

# --- Pre-configured Settings (Do not change) ---
# Enables the Vertex AI integration.
export ANTHROPIC_VERTEX_ENABLE="true"
# Specifies the required GCP region for Claude on Vertex AI.
export ANTHROPIC_VERTEX_REGION="us-east5"
# Disables prompt caching as recommended for some use cases.
export DISABLE_PROMPT_CACHING="1"

# --- Optional Settings (Uncomment to override defaults) ---
# The service defaults to the latest Claude models.
# Uncomment these lines only if you need to specify different models.
# export ANTHROPIC_VERTEX_MODELS="claude-3-opus@20240229,claude-3-sonnet@20240229"
# export ANTHROPIC_VERTEX_DEFAULT_MODEL="claude-3-sonnet@20240229"